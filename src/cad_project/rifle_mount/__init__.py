"""Magnetic rifle barrel mount for a safe: two parts (base + arm), threaded.

See ``specs/rifle-mount/spec.md`` for the human-readable specification and
``specs/rifle-mount/parameters.yaml`` for the authoritative machine
parameter source. This model is independent of ``cad_project.model``
(the mounting bracket) — it reuses the generic
``cad_project.measurements``/``exports``/``rendering`` modules but has its
own parameters, geometry, validation, and CLI.
"""
